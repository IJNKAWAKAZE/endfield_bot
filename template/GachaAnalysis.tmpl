<!DOCTYPE html>
<html lang="zh-cn">
<head>
    <meta charset="UTF-8">
    <title>寻访分析 - 终末地</title>
    <style>
        @font-face {
            font-family: 'NotoSansHans';
            src: url('/assets/font/NotoSansHans-Regular.ttf');
        }

        body {
            margin: 0;
            padding: 20px;
            font-family: 'NotoSansHans', "Microsoft YaHei", sans-serif;
            background-color: #1a1a1a;
            color: #e0e0e0;
            display: flex;
            justify-content: center;
        }

        #main {
            width: 800px;
            background-color: #242424;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding-bottom: 20px;
        }

        .title-group h1 {
            margin: 0;
            font-size: 32px;
            color: #fff;
            letter-spacing: 2px;
            text-transform: uppercase;
            font-weight: 900;
            line-height: 1.2;
        }

        .title-group p {
            margin: 0;
            font-size: 14px;
            color: #666;
            letter-spacing: 1px;
            font-weight: 300;
        }

        .user-info {
            text-align: right;
            display: flex;
            flex-direction: column;
            align-items: flex-end;
        }

        .user-name {
            font-size: 28px;
            font-weight: 900;
            color: #ffb400;
            margin-bottom: 4px;
            letter-spacing: 0.5px;
            text-shadow: 0 2px 10px rgba(255, 180, 0, 0.15);
        }

        .user-detail {
            font-size: 12px;
            color: #888;
            display: flex;
            align-items: center;
            gap: 8px;
            background: rgba(0, 0, 0, 0.2);
            padding: 4px 10px;
            border-radius: 100px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .server-badge {
            color: #ddd;
            font-weight: bold;
            padding-right: 8px;
            border-right: 1px solid #444;
        }

        .role-id {
            font-family: 'Consolas', monospace;
            color: #aaa;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }

        .summary-card {
            background-color: #2d2d2d;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            border: 1px solid #3d3d3d;
        }

        .summary-card h3 {
            margin: 0;
            font-size: 14px;
            color: #888;
            text-transform: uppercase;
        }

        .summary-card .value {
            font-size: 32px;
            font-weight: 800;
            margin: 10px 0;
            color: #fff;
        }

        .summary-card.highlight .value {
            color: #ffb400;
        }

        .pool-section {
            margin-bottom: 30px;
        }

        .pool-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
            padding-left: 5px;
            border-left: 4px solid #ff4e00;
        }

        .pool-header.weapon {
            border-left-color: #00b4ff;
        }

        .pool-header h2 {
            margin: 0;
            font-size: 20px;
            color: #fff;
        }

        .pool-stats {
            display: flex;
            gap: 20px;
            margin-bottom: 15px;
            font-size: 13px;
        }

        .stat-item {
            background: #333;
            padding: 3px 10px;
            border-radius: 4px;
            color: #ccc;
        }

        .record-table {
            width: 100%;
            border-collapse: collapse;
            background-color: #2a2a2a;
            border-radius: 8px;
            overflow: hidden;
            font-size: 14px;
        }

        .record-table th {
            text-align: left;
            background-color: #333;
            padding: 12px 15px;
            color: #888;
            font-weight: normal;
        }

        .record-table td {
            padding: 10px 15px;
            border-bottom: 1px solid #333;
        }

        .rarity-6 { color: #ffb400; font-weight: bold; }
        .rarity-5 { color: #ffea00; }
        .rarity-4 { color: #c384ff; }
        .rarity-3 { color: #888; }

        .pity-info {
            background: rgba(255, 78, 0, 0.1);
            color: #ff4e00;
            padding: 2px 6px;
            border-radius: 4px;
            font-size: 11px;
            margin-left: 8px;
        }

        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 12px;
            color: #555;
        }

        /* New Styles for separated layout */
        .section-title {
            color: #fff;
            font-size: 18px;
            margin-bottom: 15px;
            padding-left: 10px;
            border-left: 4px solid #888;
        }

        .char-title {
            border-left-color: #ffb400;
        }

        .weapon-title {
            border-left-color: #00b4ff;
        }

        .subsection-title {
            font-size: 16px;
            color: #aaa;
            margin: 20px 0 10px 0;
        }

        .recent-section {
            margin-bottom: 30px;
        }

        .weapon-pity {
            color: #00b4ff !important;
            background: rgba(0, 180, 255, 0.1) !important;
        }
    </style>
</head>
<body>
    <div id="main">
        <div class="header">
            <div class="title-group">
                <h1>寻访数据分析</h1>
                <p>Arknights: Endfield Gacha Analysis</p>
            </div>
            <div class="user-info">
                <div class="user-name">{{.PlayerName}}</div>
                <div class="user-detail">
                    <span class="server-badge">{{.ServerName}}</span>
                    <span class="role-id">UID: {{.RoleId}}</span>
                </div>
            </div>
        </div>

        <!-- Character Stats -->
        <div class="summary-section">
            <h2 class="section-title char-title">角色寻访统计</h2>
            <div class="summary-grid">
                <div class="summary-card">
                    <h3>总寻访</h3>
                    <div class="value">{{.CharStats.TotalCount}}</div>
                </div>
                <div class="summary-card highlight">
                    <h3>6星角色</h3>
                    <div class="value">{{.CharStats.SixStarCount}}</div>
                </div>
                <div class="summary-card">
                    <h3>平均水位</h3>
                    <div class="value">{{printf "%.2f" .CharStats.AveragePity}}</div>
                </div>
            </div>
        </div>

        <!-- Recent Characters -->
        <div class="recent-section">
            <h3 class="subsection-title">最近获得的 6星 角色</h3>
            <table class="record-table">
                <caption style="caption-side: bottom; font-size: 11px; color: #555; text-align: right; padding-top: 5px;">* 仅展示最近 20 条 6⭐ 记录</caption>
                <thead>
                    <tr>
                        <th style="width: 20%;">时间</th>
                        <th style="width: 35%;">卡池</th>
                        <th style="width: 30%;">名称</th>
                        <th style="width: 15%;">消耗</th>
                    </tr>
                </thead>
                <tbody>
                    {{range .RecentCharPulls}}
                    <tr class="rarity-6">
                        <td>{{.TimeStr}}</td>
                        <td>{{.PoolName}}</td>
                        <td>{{.ItemName}}</td>
                        <td><span class="pity-info">{{.Pity}}</span></td>
                    </tr>
                    {{end}}
                    {{if not .RecentCharPulls}}
                    <tr><td colspan="4" style="text-align: center; color: #555;">暂无记录</td></tr>
                    {{end}}
                </tbody>
            </table>
        </div>

        <div style="margin-top: 40px; border-bottom: 2px solid #3d3d3d; margin-bottom: 30px;"></div>

        <!-- Weapon Stats -->
        <div class="summary-section">
            <h2 class="section-title weapon-title">武器寻访统计</h2>
            <div class="summary-grid">
                <div class="summary-card">
                    <h3>总寻访</h3>
                    <div class="value">{{.WeaponStats.TotalCount}}</div>
                </div>
                <div class="summary-card highlight" style="border-color: #00b4ff; color: #00b4ff;">
                    <h3 style="color: #00b4ff;">6星武器</h3>
                    <div class="value" style="color: #00b4ff;">{{.WeaponStats.SixStarCount}}</div>
                </div>
                <div class="summary-card">
                    <h3>平均水位</h3>
                    <div class="value">{{printf "%.2f" .WeaponStats.AveragePity}}</div>
                </div>
            </div>
        </div>

        <!-- Recent Weapons -->
        <div class="recent-section">
            <h3 class="subsection-title">最近获得的 6星 武器</h3>
            <table class="record-table">
                <caption style="caption-side: bottom; font-size: 11px; color: #555; text-align: right; padding-top: 5px;">* 仅展示最近 20 条 6⭐ 记录</caption>
                <thead>
                    <tr>
                        <th style="width: 20%;">时间</th>
                        <th style="width: 35%;">卡池</th>
                        <th style="width: 30%;">名称</th>
                        <th style="width: 15%;">消耗</th>
                    </tr>
                </thead>
                <tbody>
                    {{range .RecentWeaponPulls}}
                    <tr class="rarity-6">
                        <td>{{.TimeStr}}</td>
                        <td>{{.PoolName}}</td>
                        <td>{{.ItemName}}</td>
                        <td><span class="pity-info" style="color: #00b4ff; background: rgba(0, 180, 255, 0.1);">{{.Pity}}</span></td>
                    </tr>
                    {{end}}
                    {{if not .RecentWeaponPulls}}
                    <tr><td colspan="4" style="text-align: center; color: #555;">暂无记录</td></tr>
                    {{end}}
                </tbody>
            </table>
        </div>

        <div style="margin-top: 50px;">
            <h2 class="section-title">各卡池详情 <span style="font-size: 12px; color: #888; margin-left: 10px; font-weight: normal;">(仅展示最近活跃的 6 个卡池)</span></h2>
        </div>

        {{range .Pools}}
        {{$type := .Type}}
        <div class="pool-section">
            <div class="pool-header {{if eq .Type "weapon"}}weapon{{end}}">
                <h2>{{.Name}}</h2>
            </div>
            <div class="pool-stats">
                <span class="stat-item">总计: {{.Total}}</span>
                <span class="stat-item">已累计: {{.CurrentPity}} 抽未出 6⭐</span>
                <span class="stat-item">6⭐率: {{printf "%.2f" .SixStarRate}}%</span>
            </div>
            <table class="record-table">
                <caption style="caption-side: bottom; font-size: 11px; color: #555; text-align: right; padding-top: 5px;">* 仅展示最近 5 条 6⭐ 记录</caption>
                <thead>
                    <tr>
                        <th style="width: 30%;">时间</th>
                        <th style="width: 50%;">名称</th>
                        <th style="width: 20%;">稀有度</th>
                    </tr>
                </thead>
                <tbody>
                    {{range .RecentSixStars}}
                    <tr class="rarity-{{.Rarity}}">
                        <td>{{.TimeStr}}</td>
                        <td>{{.ItemName}} <span class="pity-info {{if eq $type "weapon"}}weapon-pity{{end}}">{{.Pity}} 抽</span></td>
                        <td>{{.Rarity}}★</td>
                    </tr>
                    {{end}}
                    {{if not .RecentSixStars}}
                    <tr>
                        <td colspan="3" style="text-align: center; color: #555; padding: 20px;">暂无 6⭐ 记录</td>
                    </tr>
                    {{end}}
                </tbody>
            </table>
        </div>
        {{end}}

        <div class="footer">
            数据同步时间: {{.SyncTime}} | Endfield Bot
        </div>
    </div>
</body>
</html>
